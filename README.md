# Aaryan Bondekar Portfolio

Personal site for campus placements. Static HTML/CSS/JS, deployed on Vercel.

Edit `index.html`, `styles.css`, and `script.js`, then redeploy:

```bash
cd portfolio
vercel --prod
```
